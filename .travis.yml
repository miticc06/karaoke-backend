language: node_js
node_js:
  - 11
 
install:
  - npm i
  
stages:
  - Build
  - TestCoverage

jobs:
  include:
    - stage: Build
      script:
        - npm run build
    - stage: TestCoverage
      script:
        - npm run test:ci
